#include "connectors/PythonConnector.hpp"
#include <string>
#include <filesystem>


ExecutionConfig CppConnector::prepare(const std::filesystem::path& sourcePath) {
    ExecutionConfig config;
    config.RunCommand = "python3 " + sourcePath.string() + " 2>&1";

    return config;
}